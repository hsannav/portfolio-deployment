[project]
name = "portfolio-deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.13"
dependencies = [
    "datetime",
    "matplotlib",
    "numpy",
    "pandas",
    "plotly",
    "scipy",
    "streamlit",
    "yfinance",
]
